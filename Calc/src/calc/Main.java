package calc;

import java.util.ArrayList;

public class Main {
	
	public static void main(String[] args) throws Exception {
		
		/*
		double main_P = 100;
		double sub_P = 50;
		double load = 150;
		double front_R = 30;
		double rear_R = 60;
		
		FileRead r = new FileRead("test.txt");
		Preprocess p = new Preprocess(r.getResult());
		Calc c = new Calc(p.getTitle(), p.getNumber(), main_P, sub_P, load, front_R, rear_R);
		FileWrite w = new FileWrite(c.getResult(), "result.txt");
		*/
		
		Frame f = new Frame();
		
	}

}
